# ShareTools Environment Configuration
# Copy this file to .env and fill in actual values

# Database Configuration (for MySQL)
DB_NAME=sharetools_db
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_HOST=localhost
DB_PORT=3306

# Django Configuration
SECRET_KEY=your-secret-key-here
DEBUG=True

# Allowed Hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# Instructions for MySQL setup:
# 1. Install MySQL client: pip install mysqlclient
# 2. Create a MySQL database named 'sharetools_db'
# 3. Update the database settings in config/settings.py
# 4. Copy this file to .env and update the values
# 5. Run: python manage.py migrate 